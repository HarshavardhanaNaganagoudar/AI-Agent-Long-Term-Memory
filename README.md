# AI-Agent-Long-Term-Memory
An AI agent with persistence memory using FilesystemBackend. A simple, sentence-based memory system instead of complex structured logic.

---

# Architecture flow
Local filesystem persistence: This gives the deep agent access to local machine’s filesystem. Can specify the root directory that the agent has access to.

![Architecture](/architecture_flow.png)
